This is a super simple PHP application for sharing a single folder of files. It does not support browsing subfolders yet. The downloads are secured using usernames and passwords defined in config.php. No database is required. Just rename and edit config-dist.php.
